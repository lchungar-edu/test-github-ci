name: CI github actions
on:
    push:
        branches: [main]
jobs:
    test:
        runs-on: ubuntu-latest

        steps:
            - name: Hello world
              run: echo "Hello world"
            - name: Listar archivos
              run: ls -lha

